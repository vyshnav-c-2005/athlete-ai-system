from django.urls import path
from . import views
from django.http import HttpResponse

urlpatterns = [
    # path('add/', views.add_meal, name='add_meal'),
    # path('list/', views.list_meals, name='list_meals'),
]
